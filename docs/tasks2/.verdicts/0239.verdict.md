# Task 0239 Verification Verdict

**Date:** 2026-07-09  
**Mode:** `--auto --focus all --fix all --force`  
**Status:** PASS — all requirements and acceptance criteria MET

## Summary

Task 0239 was spec-only with zero implementation code. The `--fix all` pass implemented the full feature across two repos:
- **ts-libs** (`@gobing-ai/ts-ai-runner`): model health probe infrastructure + executor-aware DoctorRunner
- **spur-new**: CLI doctor table MODEL column, R6/AC5 warning emission, artifact diagnostic

## Requirements Traceability

| ID | Requirement | Verdict | Evidence |
|----|------------|---------|----------|
| R1 | Executor-aware doctor: each executor gets its own row; modelStatus null when no model override | MET | `doctor-runner.ts:148-164` (runAllWithExecutors), `doctor-runner.ts:175` (runOne), `modelStatus = executor.model ? await this.probeModel(...) : null` |
| R2 | ModelHealthProbe interface with provider registry | MET | `model-health-probe.ts:37-60` (ModelHealthProbe, ModelHealthProbeRegistry) |
| R3 | OmpModelProbe: pure HTTP client, receives apiKey via ProbeConfig | MET | `model-health-probe.ts:105-216` (OmpModelProbe), no env access in probe |
| R4 | MODEL column in doctor table with em-dash for no-model | MET | `agent-service.ts:643-653` (renderModelStatus returns `—` for null/undefined), `agent-service.ts:673` (always renders model column) |
| R5 | Probe uses AbortController for bounded timeout (10s default) | MET | `model-health-probe.ts:155-170` (AbortController + DEFAULT_PROBE_TIMEOUT_MS) |
| R6 | Advisory warnings for quota_exhausted/unavailable; does NOT block | MET | `agent-service.ts:192-201` (warning loop via output.error, exit code unchanged at line 211) |
| R7 | JSON output includes modelStatus | MET | `agent-service.ts:204` (toJson includes full results), test at agent-service.test.ts:324-348 |
| R8 | writePartialWorkArtifact includes executor+model diagnostic | MET | `agent-run.ts:131` (passes model), `agent-run.ts:194+209+216` (header + body lines) |
| R9 | Provider→apiType mapping: zai/deepseek/minimax=openai, volc=anthropic | MET | `model-health-probe.ts:81-86` (OMP_PROVIDERS config) |
| R10 | All tests use mocks — no real API calls | MET | All test files use mock doctor runners and fake executors; no network calls |

## Acceptance Criteria Traceability

| ID | Criterion | Verdict | Evidence |
|----|-----------|---------|----------|
| AC1 | `spur agent doctor` probes each executor's model health | MET | `doctor-runner.ts:159-161`, `agent-service.ts:184-185` (threads executors to DoctorRunner) |
| AC2 | MODEL column shows compact status; `—` when no model override | MET | `agent-service.ts:643-644` (returns `—` for null/undefined), test at agent-service.test.ts:284-305 |
| AC3 | Exit code unaffected by model health | MET | `agent-service.ts:211` — exit code only checks `!usable && tier === 1` |
| AC4 | Probe timeout bounded (10s default, configurable) | MET | `model-health-probe.ts:88` (DEFAULT_PROBE_TIMEOUT_MS=10000), `doctor-runner.ts:73` (probeTimeoutMs option) |
| AC5 | Warning emitted for quota_exhausted/unavailable | MET | `agent-service.ts:192-201`, tests at agent-service.test.ts (AC5 warning tests) |
| AC6 | JSON envelope includes modelStatus per agent | MET | `agent-service.ts:204`, test at agent-service.test.ts:324-348 |
| AC7 | Unknown provider returns `{status:'unknown', detail:'no probe registered...'}` | MET | `doctor-runner.ts:186-191`, test at doctor-runner.test.ts:397-423 |
| AC8 | No real API calls in test suite | MET | All tests mock DoctorRunner and use FakeExecutor |

## Gate Results

| Gate | Result |
|------|--------|
| `bun run lint` (biome + tsc) | PASS — 446 files clean, 5 packages typecheck |
| `bun run test` | PASS — 2556 tests, 0 fail, 7089 expect() calls |
| `bun run test-cf` | PASS — 1 test, 0 fail |
| `bun run build` | PASS — all workspaces built |
| ts-libs `bun run lint` | PASS |
| ts-libs `bun run test` | PASS — 1555 tests, 0 fail |

## Implementation Notes

- **ts-libs `bun link` workaround**: ts-ai-runner is rebuilt locally and dist files copied to `.bun` cache. This is a temporary dev workaround until the package is published with a new semver version. Per AGENTS.md dependency-source rule, this must be documented.
- **modelStatus null vs undefined**: R1 spec says `null` for no-model executors. Implementation explicitly sets `result.modelStatus = null` (not `undefined`) so JSON output includes the field as `null`.

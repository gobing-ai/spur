---
schema_version: 1
id: "I1"
name: "dev-idea: drop --design force path + idea-evaluation taste gate"
status: verifying
priority: P1
tags: []
created_at: "2026-07-28T03:21:17.944Z"
updated_at: "2026-07-28T03:37:32.267Z"
---

# I1: dev-idea: drop --design force path + idea-evaluation taste gate

## Goal
Ship a working `/sp:dev-idea` path that (1) removes the `--design` force option so Design is default-on with only `--skip-design` as opt-out, and (2) inserts an idea-evaluation taste gate after discovery so operators approve or reject an enhanced idea before any feature is created.
## Scope
- In:
  - `plugins/sp/commands/dev-idea.md` surface (arg-hint, usage, design var contract)
  - `config/workflows/idea-pipeline.yaml` / `.spur/workflows/idea-pipeline.yaml` — drop `design=force`; insert idea-eval state after discovery
  - Idea-evaluation report template + HITL approve/reject → continue or `cancelled`
  - Docs that describe the idea design package / force path (`dev-operations.md`, planning-workflow, tests, README as needed)
  - Taste-gate semantics under `--auto` (still pauses, like design-approval)
- Out: see **Out of scope** on the map (Notes)
## Acceptance Criteria

```gherkin
Feature: dev-idea: drop --design force path + idea-evaluation taste gate

  Scenario: Basic acceptance
    Given a precondition
    When an action
    Then an expected outcome
```

## Tasks

<!-- AUTO-GENERATED by spur feature refresh -->
| WBS | Task | Status |
| --- | ---- | ------ |
| 0360 | Inventory --design and design=force touchpoints on the idea path | todo |
| 0361 | Prototype the idea-evaluation report template (urgency/necessity/premises/pros-cons) | todo |
| 0362 | Decide idea-eval agent ownership, artifacts, reject wiring, and re-entry flags | todo |
| 0363 | Design idea-pipeline YAML insertion for post-discovery idea-eval taste gate | todo |
<!-- END AUTO-GENERATED -->

## Notes
**Destination (map)**

Shippable implementation: `/sp:dev-idea` no longer exposes `--design`; `idea-pipeline` design var is `auto|skip` only; after discovery the pipeline authors an idea-evaluation report (urgency/necessity 0–5, premises, pros/cons/alternatives, enhanced idea) and pauses for operator approve/reject before feature-create. Approve continues the existing pipeline; reject cancels the run without creating a feature.

**Domain context**

- Command: `plugins/sp/commands/dev-idea.md` — thin wrapper over `idea-pipeline.yaml`
- Pipeline: `config/workflows/idea-pipeline.yaml` (symlink `.spur/workflows/idea-pipeline.yaml`)
  - Current shape: `start → discovery → feature-create → ac-generate → feature-check → system-design (conditional) → design-approval → decompose → batch-create → handoff`
  - Design var today: `auto|force|skip` (`force` = `--design`)
  - Taste gate precedent: `design-approval` (`pause: true`, not auto-clicked by `--auto` unless `design_approved=true`)
- Design package SSOT: `plugins/sp/skills/spur-dev/references/dev-operations.md` §16 idea; `planning-workflow.md` design package
- HITL brief SSOT: `plugins/sp/skills/spur-dev/references/decision-brief.md` (already lists `dev-idea` intake)
- Discovery owner: `sp:brainstorm` (design summary + `needs_design` signal → `.spur/run/idea-needs-design.json`)
- Parent feature **I** (hands-off ready) shipped the original idea pipeline; this map is the next increment

**Skills every session should consult**

- `sp:wayfinder` — one ticket per session; claim before work
- `sp:spur-cli` — feature/task CLI-gated writes only
- `sp:spur-dev` — idea op, design package, decision-brief, cross-cutting design-approval pattern
- `sp:brainstorm` — discovery contract / needs_design signal
- Workflow schema / existing idea-pipeline HITL patterns when touching YAML

**Standing preferences (operator)**

- Destination kind: **shippable implementation** (not design-doc-only)
- Idea-eval placement: **after discovery**, before feature-create
- Idea-eval is a **taste gate** — `--auto` still pauses (same family as design-approval)
- `--design` removal depth: **CLI + pipeline force path** (design var collapses to `auto|skip`)
- Design remains default-on via `auto` + `--skip-design` opt-out only
- Reject = quit workflow (`cancelled`), no feature created

**Decisions so far**

- Destination kind: shippable implementation (code + docs + tests green), not task-batch-only or spec-only.
- Idea-eval placement + auto: after discovery; taste gate (always pause under `--auto`).
- `--design` removal depth: CLI surface + idea-pipeline `design=force` path removed; var becomes `auto|skip` only. Not expanding to strip `--design` from `/sp:dev-plan` in this map.

**Not yet specified**

- Whether idea-eval is a separate `agent.run` state vs folded into discovery's final artifact (ownership: brainstorm skill vs dedicated pipeline step).
- Exact score rubrics for urgency vs necessity (what 0 vs 5 mean operationally).
- Artifact paths (e.g. `.spur/run/idea-eval-report.md`) and whether an `--idea-approved` re-entry flag is needed (mirror `design_approved`).
- How reject is wired in YAML (`hitl.confirm` → `cancelled`) and whether partial files from discovery need cleanup.
- Implement task split once contracts lock (single ship vs force-removal PR then eval-gate PR).
- Test/doc matrix updates beyond the inventory ticket's change map (command-contract, skill-structure, dogfood).
- Whether the "enhanced idea" text becomes the canonical `vars.idea` for feature-create/AC, or a sidecar only.
- Dogfood script: interactive approve, interactive reject, `--auto` still pauses.

**Out of scope**

- Removing `--design` from `/sp:dev-plan` / planning-pipeline (explicitly deferred unless destination redrawn)
- Changing the design-approval taste gate or `needs_design` seam heuristic semantics (except force-path removal)
- Executing tasks after idea handoff (idea-pipeline still stops at handoff)
- Standalone PM skill or new product-management noun
- Board/UI surfaces for idea evaluation
## History
- 2026-07-28T03:21:45.072Z backlog → active (system)
- 2026-07-28T03:37:32.267Z active → verifying (system)

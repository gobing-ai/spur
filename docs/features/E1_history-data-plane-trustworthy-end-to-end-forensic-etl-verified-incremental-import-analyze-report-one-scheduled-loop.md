---
schema_version: 1
id: "E1"
name: "History data plane trustworthy end-to-end: forensic ETL, verified incremental import, analyze/report, one scheduled loop"
status: done
priority: P2
tags: []
created_at: "2026-08-06T23:08:18.775Z"
updated_at: "2026-08-08T12:43:55.812Z"
---

# E1: History data plane trustworthy end-to-end: forensic ETL, verified incremental import, analyze/report, one scheduled loop

## Goal
**Destination:** `spur history` import → analyze → report produces data trustworthy enough to
diagnose a real agent session, proven by one scheduled morning import→report loop and one ad-hoc
forensic query answering "which tool loop burned this session".

This is a **wayfinder map** (see `## Notes`). Investigation tickets resolve one per session; implementation
work graduates into tasks as the fog clears. The map closes only on
`/sp:dev-verifyall --feature E1 --fix all` reporting `Shippable: PASS`.
## Scope
### In

- Forensic ETL contract: tool calls, per-step durations, model, usage rollup, session/turn linkage.
- Sources at full fidelity: **claude, codex, pi, omp, agy, grok**.
- `spur history import` — full + incremental + ad-hoc (single file / single session), machine-parsable
  `--json` at real volume.
- Incremental correctness: `history_import_checkpoint` / `history_import_ledger` verified against
  real append-only growth.
- `spur history analyze` — JSON query-result artifact.
- `spur history report` — render from that artifact (currently a stub, `apps/cli/src/commands/history.ts:39`).
- One scheduled morning loop: import yesterday's sessions → analyze → report.

### Out of scope

- Source support for **gemini, opencode, antigravity-ide, openclaw, hermes** — deferred by operator
  ruling (2026-08-06); the ETL contract must leave adding them mechanical.
- Self-evolution agentics and rewiring `sp:issue-finding` / `/sp:dev-findissue` to read the DB instead
  of raw JSONL. That is the follow-on map this one makes possible.
- Board / web visualization of history analytics.
- Reconstructing the lost 0451 diagnostic report from yesterday's transcripts — a use of this
  capability, not part of building it.
## Acceptance Criteria
```gherkin
Feature: History data plane trustworthy end-to-end

  Scenario: R1 — forensic records survive import for every in-scope source
    Given real history files for claude, codex, pi, omp, agy, and grok
    When spur history import runs against each source
    Then imported records carry tool calls, model, usage, and session/turn linkage
    And the per-source import yield is materially above the 0.9%-1.0% baseline measured 2026-08-06

  Scenario: R2 — incremental import is exactly-once over append-only files
    Given a source file already imported to line N
    When lines are appended and spur history import --mode incremental runs again
    Then only the appended lines are imported
    And history_import_checkpoint records the new last_imported_line
    And no duplicate record_hash is written to history_import_ledger

  Scenario: R3 — machine-readable output survives real volume
    Given an import scanning hundreds of thousands of lines with many rejected records
    When spur history import --json runs
    Then the command emits parsable JSON with bounded error reporting

  Scenario: R4 — ad-hoc import targets one session
    Given a single session identifier or file path
    When spur history import runs against it
    Then only that session is imported and it is queryable immediately

  Scenario: R5 — analyze emits a JSON artifact that report renders
    Given imported forensic records
    When spur history analyze runs
    Then it writes a JSON query-result artifact
    And spur history report renders that artifact without re-querying the database

  Scenario: R6 — one scheduled loop runs unattended
    Given the scheduled morning job is configured
    When it fires
    Then yesterday's sessions across all in-scope sources are imported, analyzed, and reported
    And a failure in one source does not abort the others

  Scenario: R7 — the capability answers a real forensic question
    Given a session known to have burned time in a tool loop
    When the operator queries the imported data ad hoc
    Then time cost, token cost, and tool calls are attributable by step
```
## Tasks

<!-- AUTO-GENERATED by spur feature refresh -->
| WBS | Task | Status |
| --- | ---- | ------ |
| 0455 | ETL contract: what is a forensic history record? | done |
| 0456 | Source inventory: locate and characterize history output for pi, omp, agy, and grok | cancelled |
| 0457 | Verify checkpoint and ledger correctness under real append-only growth | done |
| 0458 | Map claude and codex record types onto the forensic contract | cancelled |
| 0459 | Decide where per-source forensic mapping lives: upstream SourceDefinition vs transform hook | cancelled |
| 0460 | Define the analyze query surface and its JSON artifact contract | cancelled |
| 0461 | Choose the scheduling surface for the morning import-analyze-report loop | cancelled |
| 0462 | Decide the ingestion path model: Spur-launched run sessions vs ambient agent history | cancelled |
| 0463 | Source discovery and field map for all six agents: claude, codex, pi, omp, agy, grok | done |
| 0464 | Consumption surface: analyze artifact, report rendering, and the scheduled loop | done |
| 0465 | Normalize history import source_file via realpath and harden line-number checkpoints | done |
| 0466 | Implement the forensic ETL contract in ts-llm-jsonl-importer: per-entry targetTable, history_message + history_tool_call schema, six source mappers | done |
| 0467 | Fix analytics SOURCE_TABLES allowlist to include omp, grok, and agy history ETL tables | done |
| 0468 | Fix issues found in 0466 forensic ETL implementation session: Bun link, TS strict, test coverage, analyze command | done |
| 0469 | Implement spur history report as a pure artifact renderer with markdown sidecar and staleness banner | done |
| 0470 | Add spur history --source all fan-out with per-source failure isolation and a spur history daily command | done |
| 0471 | Declare history.* system events and install the launchd agent for the nightly history loop | done |
| 0474 | Cut spur history analyze over to SQL aggregation with the forensic query set and versioned JSON artifact | done |
<!-- END AUTO-GENERATED -->

## Notes
### Map protocol

Wayfinder map (`sp:wayfinder`). Load this section first every session. **One ticket per session.**
Claim with `spur task update <wbs> wip` before any work. Record the answer in the task body, then
append one line to `### Decisions so far`. Graduate newly-specifiable fog into fresh child tasks and
delete it from `### Not yet specified`.

Skills every session should consult: `sp:spur-cli` (corpus verbs), `sp:source-driven-development`
(the JSONL shapes are undocumented — verify against real files, never from memory),
`sp:sys-architecture` (upstream-vs-Spur placement calls).

**Route.** All four investigation tickets — **0463**, **0457**, the keystone **0455**, and the
consumption surface **0464** — are `done`. **The map's investigation phase is complete; nothing on it
is foggy any more.** What remains is implementation only:

- **0466 Implement the forensic ETL contract** — graduated from 0455. Extend `custom` split with a
  per-entry `targetTable`, add `history_message` + `history_tool_call` to `HISTORY_IMPORT_SCHEMA_SQL`,
  write the six source mappers. Upstream in `@gobing-ai/ts-llm-jsonl-importer` via `bun link`.
  Size it before starting — if it exceeds the force-decompose guard, split by seam (extension +
  schema first, mappers second), never one task per source.
- **0465 Normalize source_file via realpath + harden checkpoints** — graduated from 0457 P1/P2;
  independent, does not block 0466.
- **0464 Consumption surface** — `done` 2026-08-07. Graduated **0467–0471** (created and wired below).

**Implementation order.** Two roots can start immediately; the rest is one chain plus one join.

```
0467  (independent — start now, ~1 session)
0465 ──────────────┐
0466 ── 0474 ── 0469
          └── 0470 ── 0471
                 ↑
              (0465)
```

- **0467 Fix `SOURCE_TABLES` allowlist** — no dependencies. `analyze` and workflow run-cost are blind
  to omp/grok/agy today and `agent.default` is `omp`, so this is a live wrong-answer bug. Smallest
  ticket on the map; do it first regardless of what else is in flight.
- **0465 Realpath + checkpoint hardening** — no dependencies. Gates 0470, because under `--source all`
  the path-identity defect hits every source on every run.
- **0466 Implement the forensic ETL contract** — no dependencies. The keystone; everything downstream
  reads the tables it populates. **ADR owed before it lands** (0455 R7).
- **0474 analyze → SQL** — needs 0466's rows.
- **0469 report renderer** — needs 0474's artifact. Sequence right after it so the artifact is never
  write-only.
- **0470 `--source all` + `spur history daily`** — needs 0465 and 0474.
- **0471 `history.*` events + launchd** — needs 0470. Closes the loop; the events are the only
  in-harness evidence a run happened, since the scheduler is external.

An ADR is owed before 0466 lands (0455 R7): the `custom` split extension plus the two-table forensic
shape change a published package's extension model. Map closes only on
`/sp:dev-verifyall --feature E1 --fix all` → `Shippable: PASS`.

**Consolidated 2026-08-06** after operator review: the map was first charted with 8 tickets, which
over-sliced the work and — worse — split claude and codex out of the source inventory into their own
downstream ticket, despite being the primary agents. Tickets 0456, 0458, 0459, 0460, 0461, and 0462
are `cancelled`; their content lives in 0463 (discovery, all six sources, both ingestion paths), 0455
(contract + ingestion decision + mapping placement), and 0464 (analyze + report + scheduling). Read
the live four only; the cancelled bodies are superseded, not archived reference.

**Expected `feature check` and `task check` warnings — do not "fix" these.**

`spur feature check E1` emits DD-09 orphan warnings for destination scenarios not yet covered by a
linked task, and implementation tasks emit `L4 uncovered-task-scenario` warnings for their own
implementation-specific scenarios. Both are correct and both are warnings, not errors — every task
still reports PASS.

**Never clear them by editing E1's `## Acceptance Criteria`.** The destination AC describes the
shipped capability and is what the map closes against; investigation and implementation tasks
legitimately carry scenarios at a different altitude. Polluting the feature AC with task-level
scenarios corrupts the close-out gate.

The legitimate way to reduce an orphan is for a task that genuinely delivers a destination scenario to
carry that scenario's title verbatim in its own AC. Task 0466 does this for R1 — which is why the
orphan count is 6, not 7. One task may carry several destination scenarios; **splitting a task 1:1
with scenarios to quiet the check is the failure this map already corrected once.**

### Ground truth (measured 2026-08-06, dry-run on this machine)

`spur history import --dry-run` against real history roots:

| source | files | lines scanned | records imported | yield |
| --- | --- | --- | --- | --- |
| claude | 326 | 90,411 | 826 | **0.9%** |
| codex | — | 224,055 | 2,141 | **1.0%** (221,911 validation errors) |
| pi | — | 1,476,470 | 1,023 | **0.07%** (1,311,401 parse errors) |
| gemini / opencode / antigravity / openclaw | 0 | 0 | 0 | roots match nothing |

Root cause of the ~99% loss: `ts-llm-jsonl-importer@0.4.19` maps sources through one generic flat
`sourceDefinition` requiring a top-level `content: string`. Claude Code JSONL nests everything under
`message.{model,usage,content[]}` across 9 record types (`mode`, `attachment`, `user`, `system`,
`last-prompt`, `assistant`, `file-history-snapshot`, `file-history-delta`, `ai-title`). Tool calls —
the substance of any step-forensics report — live in content blocks the mapping drops entirely.

**Already built upstream, do not rebuild:** `history_import_checkpoint(source, source_file,
last_imported_line, updated_at)` and `history_import_ledger(record_hash, source, source_file,
source_line, split_index, target_table, imported_at)` exist in `HISTORY_IMPORT_SCHEMA_SQL`, and
incremental mode reads the checkpoint (`src/importer.ts:59`). The operator's dedupe requirement is a
**verification** question, not a build.

**Wiring that already works:** `spur history import|analyze` are wired end-to-end
(`apps/cli/src/commands/history.ts:10`, `packages/app/src/services/history-service.ts:45`). `report`
is an explicit not-implemented stub. Root resolution is correct (registry `defaultRoots` are
home-relative, `src/importer.ts:231`).

**Two drifted registries.** `@gobing-ai/ts-ai-runner` `src/agents/shims.ts` is the canonical agent
roster — `claude`, `codex`, `gemini`, `pi`, `opencode`, `antigravity-cli` (command `agy`, alias
`antigravity`), `openclaw`, `hermes`, `omp`, `grok`. The importer's `LlmJsonlSource` union is a
separate hand-maintained list — pi, claude, codex, gemini, opencode, antigravity, openclaw — missing
**omp, grok, hermes** and naming antigravity differently. The source list should derive from the shim
roster rather than being maintained twice. (`agy` = `antigravity-cli`, confirmed at `shims.ts:198`.)

**Two ingestion paths, not one.** `packages/app/src/workflow/actions/agent-run.ts:143` routes
Spur-launched agent sessions into `.spur/run/<runId>/agent-sessions/<agent>/`, and
`discoverSessionId()` (`:408`) reads the session id back. For those runs the transcript path is
deterministic and already correlated to a `runId` — exact attribution, unlike the heuristic
`matchEtlPayloads` in `packages/domain/src/analytics/run-cost.ts:131`. But only **pi and omp** honor
`--session-dir` (`shims.ts:167,266`); claude, codex, agy, and grok ignore it and write to their
global `$HOME` locations. Ingestion-path decision is owned by **0455** (0462 cancelled; facts inventoried in 0463).

**Where transcripts actually live** (measured; note every agent dir under `$HOME` is a symlink into
`~/tools/dot_files/config/`, so any probe must follow symlinks — `find -L`, not `find`):

| source | layout | `.jsonl` count |
| --- | --- | --- |
| claude | `~/.claude/projects/<slug>/<uuid>.jsonl` | 358 |
| codex | `~/.codex/sessions/…` | 1,336 |
| pi | `~/.pi/agent/sessions/--<slug>--/*.jsonl` | 1,237 |
| omp | `~/.omp/agent/sessions/<slug>/<ts>_<uuid>/*.jsonl` | 691 |
| grok | `~/.grok/sessions/<url-encoded-cwd>/<uuid>/*.jsonl` | 418 |
| agy | `~/.gemini/antigravity-cli/brain/<uuid>/.system_generated/logs/transcript{,_full}.jsonl` | 147 |

agy also keeps `~/.gemini/antigravity-cli/conversations/<uuid>.db` — 80 SQLite databases keyed by the
same session UUIDs, so it has both a JSONL and a structured store. It is **not** under `~/.antigravity*`
(operator correction, 2026-08-06); those dirs hold no transcripts.

This explains pi's 1.3M parse errors outright: its `defaultRoots` are `['.pi/history', '.pi']`,
`.pi/history` does not exist, so the walk falls back to all of `~/.pi` and matches 3,843 stray
`*.json` files that were never transcripts. The correct root is `.pi/agent/sessions`.

### Known defects to graduate (not investigations — fold into implementation tasks)

- `--json` is unusable at real volume: `validationErrors` is an unbounded array, so 221k errors
  serialize into one blob and `--json` returned nothing for pi/codex. Any scheduled loop parses
  `--json`, so this blocks the loop.
- pi's `defaultRoots` point at a non-existent `.pi/history`, falling back to the whole `~/.pi` tree
  where the `*.json` pattern matches 3,843 non-transcript files. Correct root: `.pi/agent/sessions`
  with `*.jsonl` only (0463).
- Importer `SOURCE_DEFINITIONS` missing **omp** and **grok** (and hermes); existing `antigravity`
  root `.antigravity` is IDE config, not history — real agy transcripts are under
  `.gemini/antigravity-cli/brain` (0463).
- **Checkpoint path identity (0457 P1 → task 0465):** `source_file` not realpath-normalized;
  symlink vs real path → two checkpoint rows and duplicate imports.
- **Checkpoint rewrite skip (0457 P2 → task 0465):** line-number checkpoint silently skips content
  when a file is rewritten shorter under `--mode incremental` (theoretical for current append-only
  agents; harden or fail-loud in 0465).
- `spur history import` takes one `--source` at a time with default `pi`; a morning loop over six
  agents needs either `--source all` or six invocations.
- No ad-hoc selector for a single session (`--file` only, no `--session <id>`).

### Decisions so far

- **2026-08-06 charting — destination scope.** Map stops at a trustworthy data plane plus one live
  scheduled loop; self-evolution agentics are a follow-on map.
- **2026-08-06 charting — source parity.** claude, codex, pi, omp, agy, grok in scope; gemini,
  opencode, antigravity-ide, openclaw, hermes deferred.
- **2026-08-06 charting — fidelity target.** Full step forensics (tool calls, per-step durations,
  model, usage rollup, session/turn linkage), not token/cost rollup and not raw passthrough.
- **2026-08-06 charting — fix placement policy.** Operator ruling: upstream `@gobing-ai/ts-*`
  packages under `~/xprojects/ts-libs` are **in scope to modify**, developed against Spur via
  `bun link`. This covers both `ts-llm-jsonl-importer` and `ts-ai-runner`. Prefer fixing the facade
  over a Spur-side workaround (`AGENTS.md`). Still open, and owned by 0459: whether the declarative
  `SourceDefinition` model can *express* step forensics, or needs a transform hook.
- **2026-08-06 charting — agent identity.** `agy` is `antigravity-cli` (`shims.ts:198`), distinct
  from the deferred antigravity-ide. Its history lives under `~/.gemini/antigravity-cli/`, not
  `~/.antigravity*` (operator correction).
- **2026-08-06 charting — ticket granularity.** Operator ruling: the first 8-ticket cut was
  over-sliced. Prefer fewer, larger tickets; a ticket is a session's work, not a single question.
- **2026-08-06 — 0463 resolved.** Per-source field map complete. All six agents characterized from
  real files. Key findings: (1) pi/omp share a format with `toolCall` naming and per-message usage+cost,
  (2) codex nests everything in `payload` and uses `function_call` naming, (3) grok has two-file
  streaming format needing reconstruction, (4) agy JSONL is clean but lacks model/usage (SQLite binary),
  (5) `SOURCE_DEFINITIONS` missing omp, grok, hermes with wrong roots for pi and agy. Full field map
  with proposed roots, expected yields, and ingestion-path inventory in task 0463 Solution. Unblocks
  0455 (ETL contract) and 0457 (checkpoint verification).
- **2026-08-06 — 0457 resolved.** Checkpoint and ledger correctness verified empirically. R1 (append-only incremental) PASS. R2 (rewrite shorter) **BUG** — line-number checkpoint silently skips rewritten content. R3 (dry-run) PASS. R4 (path identity) **BUG** — symlink and real path produce 2 checkpoint rows. R4b (full-mode blast radius) PASS. **Verdict:** Line-number checkpointing is sufficient for current agents (all append-only), but `realpath()` normalization should be added to prevent path-identity duplicates. Full evidence with captured command output in task 0457 Testing section.

- **2026-08-07 — re-verify close-out.** 0463 and 0457 re-verified PASS (`--force`). 0457 defects
  graduated to **0465** (realpath + rewrite guard). Map Tasks table refreshed from task edges.
- **2026-08-07 — 0463 census widened (correction).** 0463's original census sampled **one session
  each** for pi, omp, grok, and agy — below the task's own frozen sampling rule (≥3 files, ≥2
  projects, ≥2 dates), and on exactly the four sources with no existing `SourceDefinition`. R1 was
  marked MET regardless. A re-census over the 80 most recent files per source found the shortfall is
  material: omp 7→**12** record types, grok 17→**27**, agy 12→**18**, pi 7→8. Addendum with per-type
  frequencies is appended to 0463's `### Solution`; field maps, joins, timing, and usage were not
  contradicted and stand. **Hard input for 0455: grok emits records with no `type` discriminator at
  all** (keyed only by field shape) — a type-dispatching `SourceDefinition` would silently drop them,
  the same failure class as the current ~1% yield one level down. The contract must say what happens
  to an undeterminable record: reject loudly or capture as `unknown`, never silent drop.
- **2026-08-07 — verification lesson.** A `PASS` verdict with every requirement `MET` did not mean the
  requirement's own stated bar was met. The sampling rule lived in the task's `### Design`, and verify
  checked the requirement text, not the design constraint it depended on. When a task freezes a
  method, verification has to read the method — worth carrying into `sp:code-verification` rather than
  re-learning per task.

- **2026-08-07 — 0455 resolved (keystone).** Forensic record contract fixed. **Granularity:** one row
  per message (`history_message`) + one per tool call (`history_tool_call`), with `history_etl_<source>`
  retained as raw payload of record. **Normalization:** one shared shape, so analyze/report are
  written once. **Unknown records:** captured with `disposition='unknown'` and a field-shape key,
  counted per import as the format-drift alarm — never silently skipped. **Ingestion:** both paths,
  ambient primary; Spur-launched runs carry `provenance='spur-run'` + `run_id` (exact attribution for
  pi/omp, heuristic fallback for the other four). **Placement:** extend the *existing*
  `splitConfig.mode: 'custom'` seam upstream — it already accepts an arbitrary
  `(raw) => readonly JsonObject[]`; its only gap is that `targetTable` is per-config rather than
  per-entry. Surgical extension, no new hook. **Migration:** additive — new tables only, existing
  readers keep working until 0464 cuts analyze over. **ADR owed** before implementation lands.
  Full contract in 0455 `### Design`; decision rationale in `### Q&A`. Closed with `--force-done`
  (decision ticket; no implementation in it to verify). Graduated **0466** (implementation).

- **2026-08-07 — 0464 resolved (last investigation ticket).** Consumption surface settled on measured
  evidence, not estimates. **analyze → SQL**, decided by benchmark: the current load-all-and-fold path
  allocates **+865 MB heap at 600k rows** (≈ today's real claude+codex corpus) versus a constant-memory
  `GROUP BY` at 286 ms; at the measured growth rate it crosses ~1.3 GB within a month. Ten concrete
  queries fixed, of which the two that justify this map are **loop detection** (`GROUP BY tool_name,
  args_digest HAVING COUNT(*) >= 3` — the "which tool loop burned this session" answer) and the
  **unknown-record drift alarm**. **Artifact:** versioned JSON at
  `.spur/reports/history/<date>/analyze-<selectorDigest>.json`, integer `schemaVersion` (additive
  changes do not bump; `report` refuses an unknown version), 90-day prune — and `report` **never opens
  the DB**, which is what makes the morning report reproducible. **Scheduling: launchd, not Spur's
  embedded scheduler** — the latter was disqualified three times over: ts-infra's `parseInterval`
  cannot express a daily cron (`0 7 * * *` silently degrades to a 60-second interval), it needs a
  long-lived process the run-once CLI is not, and **zero cron entries are registered anywhere in the
  monorepo**. A `spur workflow` was rejected as the driver too — the action registry has no
  `foreach`/`parallel`, so fan-out would be six hand-written `shell` steps. **Fan-out lives in
  `spur history --source all`**, not in the scheduler, so per-source isolation holds regardless of what
  invokes it; exit becomes 0/1/**2 = partial**. **The nightly window is not a date range** — 0457
  verified checkpoint resume, so the import takes no date argument and a missed night self-heals.
  Full decision in 0464 `### Design`; graduated five implementation tickets.
- **2026-08-07 — two live defects found while resolving 0464** (neither is an investigation; fold into
  implementation). (1) `SOURCE_TABLES` (`packages/domain/src/analytics/query.ts:8-16`) omits
  `history_etl_omp`, `history_etl_grok`, and `history_etl_agy` — analyze is structurally blind to three
  of six in-scope sources, and since `agent.default` is `omp`, workflow run-cost attribution silently
  under-reports **today**. Fixable now, independent of everything else. (2) An absent source imports as
  `files=0` with **exit 0**, bit-identical to a healthy no-op — under nightly fan-out an agent whose
  history path changed would report success forever while importing nothing.
- **2026-08-07 — ledger belief corrected.** `system_events` was previously characterized as ~90% prune
  heartbeat with no workflow rows. That is **stale**: the live 15,794-row ledger carries current
  `workflow.*` rows (489 `workflow.action.start`, last 2026-08-06). The real gap is narrower and was
  verified rather than assumed — **`history.*` events are 0 rows and 0 of the 66 declared events**, so
  the history plane has no ledger trail at all. Since the scheduler is external to Spur, those events
  are the only in-harness signal the loop ran; they ship with the daily command, not as a follow-up.
- **2026-08-07 — 0464's graduated tickets created and wired.** **0467** `SOURCE_TABLES` allowlist fix
  (independent), **0474** analyze→SQL (needs 0466), **0469** report renderer (needs 0474), **0470**
  `--source all` + `spur history daily` (needs 0465 and 0474), **0471** `history.*` events + launchd
  (needs 0470). All five carry Background, Requirements, and Acceptance Criteria derived from 0464
  `### Design`, pass `spur task check` with 0 errors, and have their blocking edges in frontmatter.
  **The map's investigation phase is closed — every remaining ticket is implementation.**

### Not yet specified

- **Retention and volume — narrowed, not closed.** 0464 measured the *source* side: ~590k lines /
  749 MB across claude+codex today, growing ~10.4 MB/day (~+280k lines/month), and set a 90-day prune
  on the report artifacts. Still unspecified is the *table* side: `history_tool_call` row growth
  (omp alone: 247 tool calls in one 489-line session), a pruning policy for the forensic tables, and
  index tuning under real load. Needs rows on disk — graduate after the ETL implementation lands.
- **Redaction posture.** `DEFAULT_REDACTION_RULES` exists upstream; whether it holds against real
  transcripts carrying secrets, paths, and tool output is unmeasured. The contract narrows the
  surface (`args_digest`, never raw tool args) but does not settle the rules themselves.
- **agy model and usage.** Live in `conversations/<uuid>.db` protobuf/blob columns; NULL for agy in
  v1 by decision. Revisit only if agy cost attribution turns out to matter.
## History
- 2026-08-06T23:18:19.322Z backlog → active (system)
- 2026-08-08T08:05:56.640Z active → verifying (system)
- 2026-08-08T12:43:55.812Z verifying → done (system)

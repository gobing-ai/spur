# D61 closure — 2026-09-06

> Post-commit follow-up: the operator committed this closure as `5692bc2b3`.
> The uncommitted-tree section below is historical; current checks and final corrections are in
> [D61 pre-release readiness](2026-09-06-d61-release-readiness.md).

All 13 linked tasks (0765–0777) have derived PASS verdicts and status done. The deterministic
verifyall rollup is PASS; D61's strict effective-done check passes without findings. Feature sync
applied the guarded active → verifying → done hops and refreshed its task roster/index.
This supersedes the earlier [failed verification report](2026-09-06-d61-verification.md).

## ADR-108 correction

The original ADR at `f85094a7f16e24a9ac9b5a36b1a76b1525b2d5e5` retains explicit, unsuppressed
corpus audits. The incorrect runtime/derived-doc interpretation removed the audit itself.
The dated correction preserves the original decision: simplify routine validation and remove
suppression snapshots/composition mirrors, not the operator's ability to audit.

- Restored `bun run corpus-check` / `spur task check --corpus`; preserved JSON shape and scope.
  All findings remain visible: warnings alone pass; required errors fail; baseline counters stay zero.
- Routine package gates, wrapup and post-merge hooks no longer invoke whole-corpus audits.
  Wrapup defaults to the affected-feature check. Baselines and regenerators remain deleted.
- Required references and effective-done proof/scenario/rollup checks cannot be suppressed.
- Remediation requires both task and run mutation policies to permit code. Classification-only
  tasks halt before automatic fixall dispatch. Bounded retry/monitor guidance and stale evidence
  handling are documented; existing configured auto routing is preserved, not operator-reordered.
- Updated ADR/architecture/design/process guidance and canonical templates. Two stale
  “not implemented” labels were corrected after a source/document comparison.

## Evidence and limits

- Final comprehensive gate: **7458 tests pass, zero failures**, 416 files; lint, all seven workspace
  typechecks and 44+2 rules pass (exit 0). All 13 task strict-core checks pass without findings;
  `git diff --check` passes. The routine-caller search finds only the explicit package entrypoint.
- Previous full gate: 7457 tests pass, zero failures; lint, seven workspace typechecks and 44+2
  rules pass. `SPUR_WORKFLOW_RUN_ACTIVE=1 bun run test` independently passes the same 7457 tests,
  while the explicit nested-workflow refusal test still passes.
- `bun run test-cf`: one test passes. Root build and dedicated CLI bundle build pass; the web
  build reports only its chunk-size advisory. All 11 canonical/bundled version-1 YAML files match.
- New post-merge-hook regression failed before removal and passes after removal. No assertion,
  hook execution, validation or provenance guard was bypassed to obtain green.
- [Matched measurements](2026-09-06-d61-matched-measurements.md) transparently reconstruct
  historical/current projections and real shell actions with matched fixture inputs. Success
  stdout shrank 6090 → 80 bytes; failure stdout 6090 → 1324; full logs remained 6127 bytes.
  No real model-bearing terminal runs, token/cost savings, reliable time savings or D9 fast-mode
  activation are claimed. Missing historical captures were not fabricated.
- Baseline reconstruction classifies all 299 historical keys: a=0, b=125, c=174. The old 293/6
  summary was incorrect. JSON and source provenance are in `.spur/run/d61-0773-classification.json`;
  classification is explicitly reconstructed, not presented as the missing original run.
- Fresh [dogfood report](../dogfood/2026-09-06-D61-closure-dogfood.md): PASS; machine validator
  exits 0. Real source-local audit returns exit 1 with 869 observations: **436 errors, 433 warnings**,
  zero suppressed findings. These historical corpus errors remain outside D61 repair scope;
  **the whole corpus is not clean**. Earlier capture had 880 observations, with 11 more warnings
  before task evidence updates. Required errors did not disappear.

Machine artifacts: `.spur/run/d61-verifyall-result.json`, `d61-final-feature-check.json`,
`0765-verdict.json` through `0777-verdict.json`, `d61-dogfood-audit.json` and the corresponding
answer files. Task Testing evidence is recorded through `spur task record`, never direct corpus edits.
Logs: `.spur/run/d61-closure-final-gate.log`, `d61-closure-nested-marker.log`,
`d61-closure-cf.log`, `d61-closure-build.log`, `d61-closure-bundle-final.log`.

## Working tree

The changes remain uncommitted and reviewable. This correction resumed a dirty shared tree;
no clean-start per-task commit is claimed. Pre-existing `.spur/context/pitfalls.md` changes and
task-pipeline indentation are preserved. No release, merge, push, deployment, new dependency or
external action occurred. Gitignored run artifacts are machine-local; task Testing sections and
the reports retain the human-readable evidence and reconstruction limitations.

# G61 forced verification — completed 2026-09-13

Verdict: PASS
Shippable: PASS
Feature: G61

| Task | Verdict | Requirements (MET / PARTIAL / UNMET) | Next |
| --- | --- | --- | --- |
| 0831 — delivery acceptance | PASS | 7 / 0 / 0 | No-op: already done |
| 0832 — idempotent sends | PASS | 7 / 0 / 0 | No-op: already done |
| 0833 — durable receipts | PASS | 8 / 0 / 0 | No-op: already done |
| 0834 — reconciliation | PASS | 7 / 0 / 0 | No-op: already done |

## Completed fixes

- The runner emits acceptance only after process creation. Both CLI delivery listeners require a prompt event, so a readiness/help probe cannot consume an unstarted request. Never-started requests retry within budget; started/nonzero invocations stay delivered and carry an errored run receipt.
- Request-key receipts echo the key; blank keys fail. CLI tests cover replay across fresh invocations and body conflicts, and server tests cover key forwarding.
- Run origins are stored before dispatch. Unaddressed runs receive receipts without invented occupant/message associations. A disk-reopen test verifies persistence.
- Invoke-exit notification follows durable receipt storage. A failing listener cannot erase the receipt.
- Delivered messages remain reconciliation candidates. Interrupted runs retain persisted identifiers, run status and artifacts; exhaustion reasons remain stable across repeated passes.
- Task and feature failure scenarios now match the closed 0831 Q&A/R2 decision separating delivery state from run outcome. Feature scope and storage notes reflect the implemented contract.
- Solution, Review and Testing evidence was regenerated through Spur. All four strict-core structural checks pass without findings.

## Released dependency

The original installed-package probe failed against `@gobing-ai/ts-ai-runner@0.4.63` because invocation acceptance preceded spawn. The owning fix, ts-libs commit `f967214`, is now published in 0.4.66 through the normal release process: [successful publish run](https://github.com/gobing-ai/ts-libs/actions/runs/34743863403).

Spur's catalog and lockfile resolve its ts-* dependencies to published 0.4.66. The installed-package probe passes: `acceptedBeforeSpawn: false`, `acceptedAfterNonzeroExit: true`. No release dependency remains. The generated plugin bundle carries the updated importer version.

## Verification receipts

- `bun run spur-check`: exit 0; **8,498 tests, 0 failures**; **99.21% function / 98.99% line coverage**; lint, typechecks, 45 pre-check rules and 2 post-check rules pass.
- `bun run test-cf`: exit 0; 1 test.
- `bun run build`: exit 0. CLI linked and bundled successfully.
- ts-libs source fix: `bun run spur-check` exit 0, 2,250 tests; `bun run build` exit 0; release build/publish success.
- `bun .spur/run/g61-installed-runner-probe.mjs`: exit 0 against installed 0.4.66.
- `spur task check <wbs> --strict-core --json`: all four exit 0 with no findings.
- `spur feature check G61 --json`: exit 0, no findings. All four linked tasks remain done; no missing or orphan acceptance scenario.
- `spur task verifyall-aggregate`: deterministic **PASS, 4 PASS / 0 PARTIAL / 0 FAIL**. All 29 requirements and 14 task acceptance scenarios are MET.

Per-requirement and per-AC tables are in each task's Testing section. Runtime evidence is retained under repo-root `.spur/run/`: `0831-verify-answer.txt` through `0834-verify-answer.txt`, corresponding `*-verdict.json`, `g61-summary.json`, `g61-feature-check.json`, `g61-spur-check.log`, `g61-build.log`, `g61-test-cf.log`, `g61-installed-runner-probe.log`, and `g61-publish-066.log`. These files are gitignored; the tracked task records name their paths.

`--force` re-audited the existing done tasks; `--next` made no terminal transitions. Spur changes from this completion pass remain uncommitted for the operator's next step. The upstream release required its own version commits and tags.

# G61 forced verification — 2026-09-12

Verdict: FAIL
Shippable: FAIL
Feature: G61

| Task | Verdict | Requirements (MET / PARTIAL / UNMET) | Next |
| --- | --- | --- | --- |
| 0831 — delivery acceptance | FAIL | 2 / 3 / 2 | No-op: already done; dependency release pending |
| 0832 — idempotent sends | PASS | 7 / 0 / 0 | No-op: already done |
| 0833 — durable receipts | PASS | 8 / 0 / 0 | No-op: already done |
| 0834 — reconciliation | PASS | 7 / 0 / 0 | No-op: already done |

## Fixes

- Request-key receipts now echo the key; blank keys fail. New CLI tests cover replay across fresh invocations and body conflicts, and server tests cover key forwarding.
- Run origins are stored before dispatch. Unaddressed runs receive receipts with no invented occupant or message association. A disk-reopen test verifies persistence.
- Invoke-exit notification follows durable receipt storage. A failing listener cannot erase the receipt.
- Delivered messages remain reconciliation candidates. Interrupted runs retain persisted identifiers, run status and artifacts; exhaustion reasons remain stable across repeated passes.
- Task Solution, Review and Testing evidence was regenerated through Spur; all four strict-core structural checks pass without findings.

## Remaining release dependency

The installed `@gobing-ai/ts-ai-runner@0.4.63` emits `agent.invoke.start` before process creation. A failed spawn therefore falsely acknowledges delivery. The installed-library probe still fails (exit 1).

The owning library is fixed in `/Users/robin/xprojects/ts-libs`, commit `f967214` (`fix(ai-runner): emit invocation acceptance only after spawn`). It now emits from the process executor's `onSpawn` callback. The library's complete gate passes 2,250 tests; its build also passes. No package was published and no dependency alias/link was substituted for the installed release.

Release approval was requested under the ts-libs AGENTS.md operator-initiated release policy. Recovery: publish the verified commit through its normal release command, update Spur's catalog/lockfile, run the installed-library probe and affected delivery tests, then re-verify 0831 and the feature gate. Do not infer approval from elapsed time.

## Verification receipts

- `bun run spur-check`: exit 0; 8,496 tests; 99.21% function / 98.99% line coverage; lint, typechecks and pre/post rules pass.
- `bun run test-cf`: exit 0; 1 test.
- `bun run build`: exit 0. CLI linked and bundled.
- ts-libs `bun run spur-check`: exit 0; 2,250 tests; `bun run build`: exit 0.
- `bun .spur/run/g61-installed-runner-probe.mjs`: exit 1 against installed 0.4.63; `acceptedBeforeSpawn: true`.
- `spur feature check G61 --json`: exit 1, five scenarios linked to 0831 lack a PASS verdict. No incomplete linked task and no orphan scenario were found.
- `spur task verifyall-aggregate`: deterministic FAIL, 3 PASS / 1 FAIL.

Per-requirement and per-AC tables are in each task's Testing section. Runtime evidence is retained under repo-root `.spur/run/`: `0831-verify-answer.txt` through `0834-verify-answer.txt`, corresponding `*-verdict.json`, `g61-summary.json`, `g61-feature-check.json`, `g61-spur-check.log`, `g61-build.log`, `g61-test-cf.log`, and `g61-installed-runner-probe.log`. These files are gitignored; the tracked task records name their paths.

Existing `done` statuses are historical. `--force` re-audited their evidence; `--next` made no terminal transitions. The current FAIL verdict prevents treating G61 as shippable.

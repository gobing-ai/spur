/**
 * Stage-registry module barrel.
 *
 * Re-exports the schema, types, validation helpers, and version utilities
 * for the canonical stage-registry layer (feature O, spec ticket 0282).
 */
export * from './schema';
export * from './validator';

/** Activity tab — agent-lifecycle + message-event timeline (R7). Placeholder for full adaptation. */
export default function ActivityTab() {
    return (
        <div className="p-4 text-sm text-spur-text-muted" data-activity-tab>
            Activity timeline — agent lifecycle and message events will appear here. (Adaptation of SystemEventsTab
            filtered to team/message events — pending full implementation.)
        </div>
    );
}
